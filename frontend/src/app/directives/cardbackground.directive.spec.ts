import {CardBackgroundDirective} from "./cardbackground.directive";

describe("CardBackgroundDirective", () => {
  it("should create an instance", () => {
    const directive = new CardBackgroundDirective();
    expect(directive).toBeTruthy();
  });
});
